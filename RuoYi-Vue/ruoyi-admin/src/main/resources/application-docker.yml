ruoyi:
  profile: /home/ruoyi/uploadPath

spring:
  servlet:
    multipart:
      max-file-size: ${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:100MB}
      max-request-size: ${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:100MB}
  data:
    redis:
      host: redis
      port: 6379
      database: 0
      password: ${REDIS_PASSWORD:2972515097Fzh/}
      timeout: 10s

ai:
  service:
    base-url: http://ai-service:8000
    internal-token: ${AI_INTERNAL_TOOL_TOKEN:smart-rental-ai-internal}

storage:
  provider: ${STORAGE_PROVIDER:local}

tencent:
  cos:
    secret-id: ${TENCENT_COS_SECRET_ID:}
    secret-key: ${TENCENT_COS_SECRET_KEY:}
    region: ${TENCENT_COS_REGION:ap-beijing}
    bucket-name: ${TENCENT_COS_BUCKET:homeimage-1419823100}
    presigned-expiration-seconds: ${TENCENT_COS_PRESIGNED_EXPIRATION_SECONDS:43200}
